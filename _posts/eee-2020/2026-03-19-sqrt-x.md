---
layout: distill
title: "Leetcode 69: Sqrt(x)"
description: "Computing the integer square root of a number using Binary Search."
tags: algorithms binary-search math
categories: eee-2020
date: 2018-09-20
featured: false
project: eee-2020
authors:
  - name: Jongmin Mun
    url: "https://jongminmoon.github.io"
---

The original problem is [here](https://leetcode.com/problems/sqrtx/).

# Intuition
- The goal is to find the integer part of the square root of a non-negative integer $x$.
- A naive Linear Search approach would involve squaring numbers starting from 1 until $i^2 > x$, then returning $i - 1$. This takes $O(\sqrt{x})$ time, which is slow for large values of $x$.
- Since the sequence of squares $(1^2, 2^2, 3^2, \dots)$ is **monotonically increasing** (which is equivalent to being sorted), we can optimize this to $O(\log x)$ by substituting the Linear Search for a **Binary Search**.

# Binary Search Approach

## Algorithm

Instead of an array, our search space is the range of possible integer answers from $1$ up to $x/2 + 1$.

- **Initialization:** Set `left = 1` and `right = x // 2 + 1` (since the square root of any number $x \ge 2$ is never greater than $x/2 + 1$).
- **Midpoint calculation:** At each step, compute `mid = left + (right - left) // 2`.
- **Comparison:** We compare `mid * mid` with `x`:
  - If `mid * mid == x`, we've found the exact integer root! Return `mid`.
  - If `mid * mid < x`, the square root could be `mid` or something larger. We tighten the lower bound: `left = mid + 1`.
  - If `mid * mid > x`, `mid` is too large to be the square root. We tighten the upper bound: `right = mid - 1`.

### Termination

- The loop stops when the pointers cross (`left > right`). At this point, the value of `right` will always correspond to the correct **truncated integer square root**.

## My solution

```python
class Solution:
    def mySqrt(self, x: int) -> int:
        if x == 0:
            return 0
            
        left = 1
        right = x // 2 + 1
        
        while left <= right:
            mid = left + (right - left) // 2
            
            if mid * mid == x:
                return mid
            elif mid * mid < x:
                left = mid + 1
            else:
                right = mid - 1
                
        # By the end, the 'right' pointer will correctly hold the truncated root
        return right
```

## Complexity Analysis

- **Space Complexity**: $O(1)$. We only use a few integer variables (`left`, `right`, and `mid`) indicating indices.
- **Time Complexity**: $O(\log x)$. We consistently halve the active search space starting from a size of $x/2$, taking logarithmic operations.
